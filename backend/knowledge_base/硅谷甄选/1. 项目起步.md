# 项目起步

## 1 创建项目

1. 安装包管理工具

   ```bash
   npm install -g pnpm   # 先安装 pnpm（全局工具） 
   ```

2. 创建项目文件

   ```bash
   pnpm create vue    #先选框架（vue）,有vue的生态                    ... vue@latest指最新
   pnpm create vite   #先选构建工具，再选框架，没有框架的生态
   ```

3. 安装依赖

   ```bash
   pnpm install
   ```

## 2 创建git仓库

1. 新建本地仓库

   ```bash
   git init                            #初始化新仓库
   ```


2. 添加到暂存区

   ```bash
   git add -A               #添加
   git ls-files           #查看暂存区  
   ```

3. 从暂存区到本地仓库

   ```bash
   git commit -m'注释部分'
   git log                #查看提交历史
   ```

4. 添加远程仓库（gitee上面有这两行）

   ```bash
   git remote add （远程）仓库名 地址          #上传
   git remote -v                             #查看（一般有两个）
   git push -u 仓库名 本地分支名：远程分支名    #推送（#如果两名相同，可简写）
   ```

   > U --- 未跟踪
   >
   > A --- 第一次暂存（还没有暂存版本）
   >
   > ' ' --- 提交后（内容都是最新，没有标识符）
   >
   > M --- 暂存中 （第二次，或第n次暂存）

## 3 配置规则及检查工具

> 在团队协作开发时，一般来说，这些配置是由项目管理员进行编写。
>

ESLint（TS,JS错误预防）, Prettier（代码格式化）,StyleLint（CSS,SCSS，LASS等样式表预防）在项目初始化就配置了，但还可以对其修改

### 3.1常规配置

1. 查看jsconfig.json是否配置别名

   `tsconfig.app.json`

   ```json
   {
     "compilerOptions" : {
       ...
       "paths" : {
         "@/*":["src/*"]
       }
   ```

2. 检查TS声明文件

   `env.d.ts`

   ```ts
   /// <reference types="vite/client" />
   declare module '*.vue' {
     import type { DefineComponent } from 'vue'
     const component: DefineComponent<{}, {}, any>
     export default component
   }
   ```

3. 配置项目自动启动

   `pakeage.json`

   ```json
   "scripts": {
       "dev": "vite --open",
   } 
   ```

### 3.2 ESLint（错误预防）配置

官网：[eslint 中文网](https://eslint.nodejs.cn/)

- 默认配置

  `esline.config.ts`

  ```ts
  ...
  export default defineConfigWithVueTs(
    {
      name: 'app/files-to-lint',
      files: ['**/*.{ts,mts,tsx,vue}'],    //默认只检查ts,mts,tsx,vue
    },

    globalIgnores(['**/dist/**', '**/dist-ssr/**', '**/coverage/**']),   // 忽略构建文件夹

    pluginVue.configs['flat/essential'],  // Vue 基础规则
    vueTsConfigs.recommended,             // TypeScript 推荐规则
    skipFormatting,                       // 避免与 Prettier 冲突的规则
  )

  ```

1. 修改文件范围

   ```ts
   {
     name: 'app/files-to-lint',
     files: ['**/*.{js,ts,mts,tsx,vue}'],    //添加 js 文件
   },
   ```

2. 添加自定义规则

   ```ts
   export default defineConfigWithVueTs(
     // ... 现有配置
     
     // 添加自定义规则（在最后）
     {
       rules: {
        ...
       },
     }
   )
   ```

3. 添加插件（如需要）

   ```ts
   import somePlugin from 'eslint-plugin-some-plugin'

   export default defineConfigWithVueTs(
     // ...
     {
       plugins: {
         'some-plugin': somePlugin,
       },
       rules: {
         'some-plugin/rule-name': 'error',
       },
     }
   )
   ```

### 3.3 Prettier(代码格式化)配置

- 默认配置

  ```json
  {
    "$schema": "https://json.schemastore.org/prettierrc",  // 配置的JSON Schema
    "semi": false,                                          // 行尾不加分号
    "singleQuote": true,                                    // 使用单引号
    "printWidth": 100                                       // 每行最大100字符
  }

  ```

> 因为这些命令很直接，可以直接搜索添加，例如，行宽配置、缩进配置、括号间距、箭头函数参数括号、文件类型特定配置。另外，如需要可以添加一个忽略文件（Prettiernore）

### 3.4 stylelint 配置

因为Vue官方没有默认包含该工具，需要自己配置

官网：[入门 | Stylelint 中文](https://stylelint.node.org.cn/user-guide/get-started/)

配置步骤：

1. 安装依赖

   ```bash
   pnpm add -D stylelint-config-standard-scss   # 如果你用 SCSS（推荐）
   ```

2. 通用配置

   ```json
   {
     "$schema": "https://json.schemastore.org/stylelintrc.json",
     "extends": [
       "stylelint-config-standard",
       "stylelint-config-recommended-vue",
       "stylelint-config-standard-scss", // 如果使用 SCSS
       "stylelint-config-prettier",
       "stylelint-prettier/recommended"
     ],
     "plugins": ["stylelint-order"],
     "rules": {
       // 自定义规则覆盖
     }
   }
   ```


### 3.5 配置husky

在上面我们已经集成好了我们代码校验工具，但是需要每次手动的去执行命令（`pnpm format`）才会格式化我们的代码。如果有人没有格式化就提交了远程仓库中，那这个规范就没什么用。所以我们需要强制让开发人员按照代码规范来提交。

原理：要做到这件事情，就需要利用husky在代码提交之前触发git hook(git在客户端的钩子)，然后执行`pnpm run format`来自动的格式化我们的代码。

步骤：

1. 安装husky

   ```bash
   npx husky install
   ```

2. 执行

   ```bash
   npx husky-init
   ```

   > 此时会在根目录下生成个一个.husky目录，
   >
   > 在这个目录下面会有一个pre-commit文件，这个文件里面的命令在我们执行commit的时候就会执行

3. 在`.husky/pre-commit`文件添加如下命令：

   ```
   #!/usr/bin/env sh
   . "$(dirname -- "$0")/_/husky.sh"
   pnpm run format
   ```

   当我们对代码进行commit操作的时候，就会执行命令，对代码进行格式化，然后再提交。

### 3.6 强制使用pnpm包管理器工具

- 作用：团队开发项目的时候，需要统一包管理器工具,因为不同包管理器工具下载同一个依赖,可能版本不一样,

  ​           导致项目出现bug问题,因此包管理器工具需要统一管理！！！

- 在根目录创建`scritps/preinstall.js`文件，添加下面的内容

  ```js
  if (!/pnpm/.test(process.env.npm_execpath || '')) {
    console.warn(
      `\u001b[33mThis repository must using pnpm as the package manager ` +
      ` for scripts to work properly.\u001b[39m\n`,
    )
    process.exit(1)
  }
  ```


- 配置命令（否则不会运行上面代码）

  ```json
  "scripts": {
  	"preinstall": "node ./scripts/preinstall.js"
  }
  ```

> 当我们使用npm或者yarn来安装包的时候，就会报错了。
>
> 原理就是在install的时候会触发preinstall（npm提供的生命周期钩子）这个文件里面的代码。


## 4 引入elementPlus

网址：[快速开始 | Element Plus](https://element-plus.org/zh-CN/guide/quickstart)

### 4.1引入element-plus

1. 安装elementPlus（使用包管理工具）

   ```bash
   pnpm install element-plus
   ```

2. 全局导入

   按步骤即可（在入口文件中引入，再在tsconfig.json指定全局类型）

### 4.2 引入element-plus图标库

1. 安装

   ```bash
   pnpm install @element-plus/icons-vue
   ```

2. 全局注册

   ```ts
   // 如果您正在使用CDN引入，请删除下面一行。
   import * as ElementPlusIconsVue from '@element-plus/icons-vue'

   const app = createApp(App)
   for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
     app.component(key, component)
   }
   ```

### 4.3 国际化

Element Plus 组件**默认**使用英文。 如果你想使用其他语言，请继续阅读以了解具体方法。



### 4.4 定制element-plus主题颜色

查看官方步骤：[主题 | Element Plus](https://element-plus.org/zh-CN/guide/theming)

1. 安装sass

   > 说明：因为在配置styleLint工具的时候，项目当中已经安装过sass sass-loader,因此我们再组件内可以使用scss语法！！！**不需要再安装**

   ```bash
   pnpm i sass -D 
   ```

2. 在src新建文件`styles/element/index.scss`

   在文件中写入自己的主题颜色，例如小兔鲜主题颜色：

   ```scss
   /* 只需要重写你需要的即可 */
   @forward 'element-plus/theme-chalk/src/common/var.scss' with (
     $colors: (
       'primary': (
         // 主色
         'base': #27ba9b,
       ),
       'success': (
         // 成功色
         'base': #1dc779,
       ),
       'warning': (
         // 警告色
         'base': #ffb302,
       ),
       'danger': (
         // 危险色
         'base': #e26237,
       ),
       'error': (
         // 错误色
         'base': #cf4444,
       ),
     )
   )
   ```

3. 在vite配置文件中覆盖默认主题颜色

   `vite.config.ts`

   ```typescript
   export default defineConfig({
    ...
       Components({    //配置elementPlus采用sass样式配色系统
         resolvers: [ElementPlusResolver({importStyle: "sass"})],
       })
     ],
     resolve: {...},
     css: {
       preprocessorOptions: {
         scss: {
            //自动导入定制化样式进行样式覆盖
           additionalData: `@use "@/styles/element/index.scss" as *;`,
         },
       },
     },
   })
   ```



## 5 环境变量配置

> **项目开发过程中，至少会经历开发环境、测试环境和生产环境(即正式环境)三个阶段。**
>
> **不同阶段请求的状态(如接口地址等)不尽相同，若手动切换接口地址是相当繁琐且易出错的。**
>
> **于是环境变量配置的需求就应运而生，我们只需做简单的配置，把环境状态切换的工作交给代码。**

- **开发环境（development）**

  顾名思义，开发使用的环境，每位开发人员在自己的dev分支上干活，开发到一定程度，同事会合并代码，进行联调。


- **测试环境（testing）**

  测试同事干活的环境啦，一般会由测试同事自己来部署，然后在此环境进行测试


- **生产环境（production）**

  生产环境是指正式提供对外服务的，一般会关掉错误报告，打开错误日志。(正式提供给客户使用的环境。)

> 注意:一般情况下，一个环境对应一台服务器,也有的公司开发与测试环境是一台服务器！！！

配置：

1. 项目根目录分别添加 开发、生产和测试环境的文件!（新建开发环境文件）

   > .env.development
   > .env.production
   > .env.test


2. 文件内容（分别是三个开发环境）

   ```
   #变量必须以 VITE_ 为前缀才能暴露给外部读取
   NODE_ENV = 'development'
   VITE_APP_TITLE = '硅谷甄选运营平台'
   VITE_APP_BASE_API = '/dev-api'
   ```

   ```
   NODE_ENV = 'production'
   VITE_APP_TITLE = '硅谷甄选运营平台'
   VITE_APP_BASE_API = '/prod-api'

   ```

   ```
   # 变量必须以 VITE_ 为前缀才能暴露给外部读取
   NODE_ENV = 'test'
   VITE_APP_TITLE = '硅谷甄选运营平台'
   VITE_APP_BASE_API = '/test-api'
   ```


3. 配置运行命令：package.json（配置其中第三四行到文件中）

   ```
   "scripts": {
       "build:test": "vue-tsc && vite build --mode test",        //测试环境
       "build:pro": "vue-tsc && vite build --mode production",   //生产环境
     },
   ```

通过import.meta.env获取环境变量

## 6 引入SVG图标库

官网:[iconfont-阿里巴巴矢量图标库](https://www.iconfont.cn/)

1. 查看引入方式：在官网的帮助/代码应用（[iconfont-阿里巴巴矢量图标库](https://www.iconfont.cn/help/detail?spm=a313x.manage_type_myprojects.i3.48.33363a81ILCVGD&helptype=code)）。我们使用的是font-class 引用方法。

2. 选取需要的**所有**字体图标及生成fontclass代码：

   > 1. 在首页中直接搜索即可（如购物车、搜索、分享）；
   >
   > 2. 放入购物车，直到选完为止；
   >
   > 3. 点击导航条的购物车，添加至项目
   >
   > 4. 加入或新建一个项目
   >
   > 5. 选中资源管理->我的项目->Font class
   >
   >    ​                                          ->在线查看链接->复制

3. 引入到项目中：

   将刚刚复制的链接粘贴到`index.html`

   ```html
   <link rel="stylesheet" href="//at.alicdn.com/t/font_2143783_iq6z4ey5vu.css">
   ```

4. 使用字体图标：

   在任意组件中进行嵌套`<i class="iconfont icon-xxx"></i>`，xxx是类名（也就是我之前选取的Font class）

   ```vue
   <template>
     <dd><i class="iconfont icon-home"></i> 公众号</dd>
     <dd><i class="iconfont icon-doling"></i> 微博</dd>
   </template>
   ```
> 注意：
>
> ​	这个链接是可复用的，可在不同设备正常使用

## 7 配置接口

### 7.1 配置mock

**mock简介：**

- Mock（模拟）是软件开发中创建模拟对象的技术，用于替代真实组件进行**测试**和**开发**。
- 主要作用：
  - 后端 API 未完成时，前端可独立开发
  - 模拟正常/异常数据、网络延迟、错误状态

**配置：**

按照官网安装依赖：https://www.npmjs.com/package/vite-plugin-mock

> 注意：刚出的3.0.0版本没有说明文档，需要查看2.9.8版本（导航栏选择Versions，切换版本）

1. 安装插件：

   ```powershell
   pnpm install -D vite-plugin-mock mock@2.9.8
   ```

2. 在 vite.config.js 配置文件---启用插件

   ```js
   import { viteMockServe } from 'vite-plugin-mock'

   export default ({ command })=> {        //command---检测当前环境
     return {
       plugins: [
         vue(),
         viteMockServe({
           localEnabled: command === 'serve',  //保证开发阶段可使用mock接口
         }),
       ],
     }
   }
   ```

3. 在根目录创建mock文件夹，创建需要mock数据与接口（造假接口）

   > 在npm官网搜索vite-plugin-mock查询（假接口）模板

   `mock/user.ts`

   ```ts
   //用户信息数据
   //返回两个用户信息
   function createUserList() {
       return [
           {
               userId: 1,
               avatar:
                   'https://wpimg.wallstcn.com/f778738c-e4f8-4870-b634-56703b4acafe.gif',
               username: 'admin',
               password: '111111',
               desc: '平台管理员',
               roles: ['平台管理员'],
               buttons: ['cuser.detail'],
               routes: ['home'],
               token: 'Admin Token',
           },
           {
               userId: 2,
               avatar:
                   'https://wpimg.wallstcn.com/f778738c-e4f8-4870-b634-56703b4acafe.gif',
               username: 'system',
               password: '111111',
               desc: '系统管理员',
               roles: ['系统管理员'],
               buttons: ['cuser.detail', 'cuser.user'],
               routes: ['home'],
               token: 'System Token',
           },
       ]
   }
   //对外暴露一个数组（包含两个接口）
   //登录接口、获取用户信息接口
   export default [
       // 用户登录接口
       {
           url: '/api/user/login',//请求地址
           method: 'post',//请求方式
           response: ({ body }) => {
               //获取请求体携带过来的用户名与密码
               const { username, password } = body;
               //调用获取用户信息函数,用于判断是否有此用户
               const checkUser = createUserList().find(
                   (item) => item.username === username && item.password === password,
               )
               //没有用户返回失败信息
               if (!checkUser) {
                   return { code: 201, data: { message: '账号或者密码不正确' } }
               }
               //如果有返回成功信息
               const { token } = checkUser
               return { code: 200, data: { token } }
           },
       },
       // 获取用户信息
       {
           url: '/api/user/info',
           method: 'get',
           response: (request) => {
               //获取请求头携带token
               const token = request.headers.token;
               //查看用户信息是否包含有此token用户
               const checkUser = createUserList().find((item) => item.token === token)
               //没有返回失败的信息
               if (!checkUser) {
                   return { code: 201, data: { message: '获取用户信息失败' } }
               }
               //如果有返回成功信息
               return { code: 200, data: {checkUser} }
           },
       },
   ]
   ```

4. 检查接口是否可用

   安装axios

   ```powershell
   pnpm install axios
   ```

   `main.ts`

   ```ts
   //测试
   import axios from 'axios';
   axios({
     url:'/api/user/login',
     method: 'post',
     data:{
       username:'admin',
       password:'111111'
     }
   })
   ```

   > 可以通过状态码，及其他信息检验接口是否可用

### 7.2 配置axios

网址：https://axios-http.com/zh/docs/interceptors

1. 安装axios（前面已经安装了）

2. 基础配置

   > 统一接口配置：
   >
   > 1. 实例化：接口基地址+接口超时时间
   > 2. 拦截器：

   `utils/http.js`

   ```javascript
   import axios from "axios";

   let request =  axios.create({
     baseURL: import.meta.env.VITE_APP_BASE_API,       //使用环境变量接待的 基地址
     timeout: 5000,
    
   });

   // 添加请求拦截器
   request.interceptors.request.use(function (config) {
       // 在发送请求之前做些什么
       return config;
     }, function (error) {
       // 对请求错误做些什么
       return Promise.reject(error);
     });

   // 添加响应拦截器
   request.interceptors.response.use(function (response) {
       // 2xx 范围内的状态码都会触发该函数。
       // 对响应数据做点什么
       return response.data;
     }, function (error) {
       // 超出 2xx 范围的状态码都会触发该函数。
       //定义变量：存储网络错误信息
       let message = ''
       //http状态码
       let status = error.response.status
       switch (status) {
           case 401:
               message = 'token过期'
               break;
           case 403:
               message = '无权访问'
               break
           case 404:
               message = '请求地址错误'
               break
           case 500:
               message = '服务器出现问题'
               break
           default:
               message = '网络出现问题'
               break   
       }
       //提示错误信息
       ElMessage({
           type:'error',
           messaeg
       })
       return Promise.reject(error);
     });

     export default request
   ```

3. 封装请求函数

   > 在api模块中进行封装测试
   >
   > 当然，此时就是多个api接口

   `apis/testAPI.js`

   ```javascript
   import httpInstance from "@/utils/http"

   export function getCatgory(){
       return 测试代码
   }
   ```

### 7.3 API统一管理

1. 新建api/user/index.ts

   ```ts
   import request from '@/utils/request'
   import { loginFrom, loginResponseData } from './type'

   //统一管理接口
   enum API {
       LOGIN_URL = '/user/login',
       USERINFO_URL = '/user/info'
   }

   //暴露请求函数
   //登录（提交）接口方法      提交类型                           响应类型
   export const reqLogin = (data: loginFrom) => request.post<loginResponseData>(API.LOGIN_URL, data)
   //获取用户信息接口方法
   export const reqUserInfo = () => request.get<userResponseData>(API.USERINFO_URL)
   ```

2. 数据类型管理，新建user/type.ts

   ```ts
   //登录接口携带的参数类型
   export interface loginFrom{
       username: string,
       password: string
   }

   //登录接口返回数据类型
   interface dataType{
       token: string
   }
   export interface loginResponseData{
       code: number,
       data: dataType
   }

   //服务器返回用户信息数据类型
   interface userInfo{
     userId: number
     avatar: string
     username: string
     password: string
     desc: string
     roles: string[]    //字符串组成的数组
     buttons: string[]
     routes: string[]
     token: string
   }
   interface user {
       checkUser: userInfo
   }
   export interface userResponseData{
       code: number
       data: user
   }
   ```

## 8 设置路由

1. 创建路由文件：`Login`、`NotFound`、`Home`并分别写入测试标签文字

2. 配置路由，在[带参数的动态路由匹配 | Vue Router](https://router.vuejs.org/zh/guide/essentials/dynamic-matching.html)中有现成的404配置规则

   `router/index.ts`

   ```ts
   1.分别引入这些组件
   import NotFound from '@/views/NotFound.vue'

   2.配置路由规则
   const router = createRouter({
     history: createWebHistory(import.meta.env.BASE_URL),
     routes: [
       { path: '/:pathMatch(.*)*', name: '404', component: NotFound },
     ]
   })
   ```

3. 在这些路由组件的上一级中展示，例如

   `APP.vue`

   ```vue
   <template>
     //一级路由展示区
     <router-view></router-view>
   </template>
   ```

### 8.1创建404页面组件

在element-plus直接搜索404，或者搜索Loading然后自己组装

### 8.2 配置滚动行为

- 默认情况下，Vue Router 会尽量保持滚动位置（比如你在长页面滚动到中部，然后点击链接进入新页面，再返回时，会恢复到你离开时的滚动位置）

1. 将数组摘除

   > 为了使router页面更简洁，可以将routes对象（即，路由数组）分拿出了，

   ```ts
   export const constantRoute = [
     {
       path: '/',
       name: 'home',
       component: () => import('@/views/Home/index.vue'),
     },
     ...
   ]
   ```

2. 配置滚动行为

   ```ts
   import { createRouter, createWebHistory } from 'vue-router'
   import { constantRoute } from './router'

   const router = createRouter({
     history: createWebHistory(import.meta.env.BASE_URL),
     routes: constantRoute,
     //滚动行为
     scrollBehavior() {
       return {
         left: 0,
         top: 0
       }
     }
   })

   export default router
   ```

   ​

## 9 静态资源引入

静态资源分为图片资源和样式资源

1. 图片资源一般有UI设计师提供，包括图片（背景图、404图）、icon(字体图标)、视频等，直接放入到`assets`文件夹即可

2. 样式资源一般是项目初始化的时候进行重置，需要自己手写或开源的`normalize.css`

   这个样式文件放在`slytes`文件夹中，注意：需要在入口文件引入才能生效

